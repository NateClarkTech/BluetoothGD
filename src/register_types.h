#pragma once

#include <godot_cpp/core/class_db.hpp>

void initialize_bluetooth_manager_module(godot::ModuleInitializationLevel p_level);
void uninitialize_bluetooth_manager_module(godot::ModuleInitializationLevel p_level);